/* eslint-disable no-unused-vars */
import { Given, When, Then } from "@cucumber/cucumber";

Given("I have my character sheet open", function () {
  return "pending";
});

Given("my character's base {word} is {int}", function (stat, base) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Given(
  "my character has the {word} skill at the full level with {int} bonus",
  function (skill, bonus) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

When("my {word} is {int}", function (stat, value) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

When(
  "I set my character's {word} modifier to {int}",
  function (stat, modifier) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

Then("the total {word} should be {int}", function (stat, total) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then(
  "I should see that my {word} roll has a target number of {int}",
  function (stat, targetNumber) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

Then(
  "the {word} characteristic roll should have a target number of {int}",
  function (stat, targetNumber) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

Then(
  "the {word} skill roll should have a target number of {int}",
  function (skill, targetNumber) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);
