/* eslint-disable no-unused-vars */
import { Given, When, Then } from "@cucumber/cucumber";

Then("my HTH damage dice should be {int}", function (dice) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("my Presence Attack dice should be {int}", function (dice) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("my lifting weight should be {float} {word}", function (amount, unit) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("I should have Phases on segments {listOfInt}", function (segments) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});
