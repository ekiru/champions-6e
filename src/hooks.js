const system = "champions-6e";

// DEX_CHANGE(actor, oldDex, newDex)
export const DEX_CHANGE = `${system}.character.dexChange`;

// SPD_CHANGE(actor, oldSpd, oldPhases, newSpd, newPhases)
export const SPD_CHANGE = `${system}.character.spdChange`;
