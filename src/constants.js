/**
 * Custom system-specific `mode`s for `ActiveEffect`s.
 */
export const SystemActiveEffectModes = {
  HALVED: 60,
};

export const EffectFlags = {
  UNIQUE_CATEGORY: "effect•unique",
  SUMMARY: "effect•summary",
};
